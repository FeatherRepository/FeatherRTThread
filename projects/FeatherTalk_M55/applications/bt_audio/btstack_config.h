/* btstack_config.h - M55 SBC 解码最小配置 (A0-2)
 *
 * 仅供编入 M55 的 btstack_sbc_decoder_bluedroid.c / btstack_sbc_bluedroid.c /
 * btstack_sbc_plc.c / btstack_util.c 编译: 不开 ENABLE_LOG_* (log_* 为空操作),
 * 不开 HAVE_ASSERT (btstack_assert 为空操作)。真正的协议栈配置在
 * bluetooth/port_rtthread/btstack_config.h (M33 BT Host 侧), 与本文档无关。
 */
#ifndef BTSTACK_CONFIG_H
#define BTSTACK_CONFIG_H

#endif /* BTSTACK_CONFIG_H */
