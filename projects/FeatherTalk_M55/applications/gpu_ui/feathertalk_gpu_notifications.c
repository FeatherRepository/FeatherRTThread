/* Reuse the UI-independent queue while both renderers coexist during the
 * migration.  The implementation and this wrapper are in the same repository. */
#include "../ui/feathertalk_ui_notifications.c"
